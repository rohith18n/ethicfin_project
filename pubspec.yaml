# yaml-language-server: $schema=https://json.schemastore.org/pubspec.json
name: ethicfin_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  equatable: ^2.0.7
  get_it: ^8.0.3
  dio: ^5.7.0
  shared_preferences: ^2.3.5
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  intl: ^0.20.2
  url_launcher: ^6.3.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  bloc_test: ^9.1.7

flutter:
  uses-material-design: true
